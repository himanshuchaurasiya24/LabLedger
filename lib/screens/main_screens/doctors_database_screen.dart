import 'package:flutter/material.dart';

class DoctorsDatabase extends StatelessWidget {
  const DoctorsDatabase({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Doctors Database")));
  }
}
