import 'package:flutter/material.dart';

class ReportDatabaseScreen extends StatelessWidget {
  const ReportDatabaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Reports Database")));
  }
}
