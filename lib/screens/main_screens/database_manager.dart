import 'package:flutter/material.dart';

class DatabaseManager extends StatelessWidget {
  const DatabaseManager({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Database Manager."));
  }
}
