import 'package:flutter/material.dart';

class DiagnosisTypeDatabaseScreen extends StatelessWidget {
  const DiagnosisTypeDatabaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Diagnosis Type Database")),
    );
  }
}
